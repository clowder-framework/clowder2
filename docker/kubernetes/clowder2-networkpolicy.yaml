apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  creationTimestamp: null
  name: clowder2
spec:
  ingress:
    - from:
        - podSelector:
            matchLabels:
              io.kompose.network/clowder2: "true"
  podSelector:
    matchLabels:
      io.kompose.network/clowder2: "true"
